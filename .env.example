# Public Mapbox token used for satellite, terrain, and mapped 3D buildings.
# Leave empty to use the interactive OpenStreetMap raster fallback.
# NEXT_PUBLIC_ values are exposed to the browser; use a URL-restricted public token.
NEXT_PUBLIC_MAPBOX_TOKEN=
